touch database/database.sqlite
```

Or if you're on Windows, create an empty file at `database/database.sqlite`

### Step 3: Update .gitignore

Make sure your `.gitignore` file **does NOT** ignore the SQLite file. Find this line in `.gitignore`:
```
*.sqlite
```

And change it to:
```
# *.sqlite